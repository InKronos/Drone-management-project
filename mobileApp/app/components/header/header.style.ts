import { StyleSheet } from "react-native";
import { theme } from "../../../App.style";

export const headerStyle = StyleSheet.create({

    appBack: {
        backgroundColor: "rgb(0, 97, 163)"
    },
    menu: {
        color: "white"
    }
}) 